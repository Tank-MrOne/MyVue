export const getters = {
     arrs(state){
        return state.arrs 
     },
     isFirst(state){
        return state.isFirst 
     },
     isLoading(state){
        return state.isLoading 
     },
     errMsg(state){
        return state.errMsg 
     }
}